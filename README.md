# RISC-V Arch Test

This repo contains the code for different tasks in the course.

## Commands
`make build`: Builds the elf file

`make disasm`: Generates disassembly of elf file

`make debug`: Launches Spike in debug mode for interactive debugging

`make run`: Runs the generated elf file on Spike
